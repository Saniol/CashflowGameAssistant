import 'react-native';
import React from 'react';
import BalanceView from '../../../src/balance/View';
import renderWithContexts from '../../common/renderWithContexts';

describe('<BalanceView>', () => {
    renderWithContexts(BalanceView);
});
